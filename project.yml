name: MODUTABLE
options:
  bundleIdPrefix: com.modutable
  deploymentTarget:
    iOS: "15.0"
settings:
  base:
    SWIFT_VERSION: 5.0
    TARGETED_DEVICE_FAMILY: "1,2"
    INFOPLIST_FILE: MODUTABLE/Info.plist
targets:
  MODUTABLE:
    type: application
    platform: iOS
    sources:
      - path: MODUTABLE
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.modutable.app
        PRODUCT_NAME: MODUTABLE
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_STYLE: Manual
        CODE_SIGN_IDENTITY: iPhone Distribution
        DEVELOPMENT_TEAM: 8FJX2NBYXT
